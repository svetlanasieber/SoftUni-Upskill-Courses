//The Rectangle's area is calculated when you multiplied length by width: length * width

int length = int.Parse(Console.ReadLine());
int width = int.Parse(Console.ReadLine());

int area = length * width;

Console.WriteLine(area);
